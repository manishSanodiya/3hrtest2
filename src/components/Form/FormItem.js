import React from "react";

const FormItem = (props) => {
  return (
  
      <li>
        name:{props.name}, price:{props.price}, discription:{props.discription},
        
      </li>
    
  );
};

export default FormItem;
